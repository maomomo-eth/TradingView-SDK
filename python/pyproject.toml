[project]
name = "tradingview-scanner-sdk"
version = "0.1.0"
description = "TradingView Scanner unofficial Python SDK"
requires-python = ">=3.10"
license = { text = "MIT" }

[tool.setuptools.packages.find]
include = ["tradingview*"]
